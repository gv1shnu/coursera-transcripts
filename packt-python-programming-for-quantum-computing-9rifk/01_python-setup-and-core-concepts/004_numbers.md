# Numbers

- **Course:** Python Programming For Quantum Computing
- **Module 1:** Python Setup and Core Concepts
- **Lecture #:** 4
- **URL:** https://www.coursera.org/learn/packt-python-programming-for-quantum-computing-9rifk/lecture/sYlLb/numbers
- **Extracted:** 2026-06-22 15:06:41

---

Hi. Within this lecture, we are going to officially start writing our first Python code, and we are going to work with not books. So I'm going to create a New Notebook from here. And remember, I'm going to save my Notebooks and share all of them with you in the GitHub.

But, I really recommend you to not only watch me and take notes, but also write your own code at the same time within the Jupyter Notebook. I'm going to share this with you anyway, but just use them as references, and try to write it synchronously with me, so that you can practice and learn it better. So let's create our first Notebook, and let me Zoom in a little bit, so you can see it better from here. And I'm going to change the name of it.

So, I'm going to give numbers to my Notebooks, and this is just for me to track the book, so I can share with you properly, don't worry about the numbers, I will share every Notebook after each individual lecture. So if you don't want to name this with numbers, you're, of course, welcome to do that. And, I'm going to name this Variables. That's what we're going to talk about a little bit in this lecture and the following lecture, okay.

I'm going to Rename this Notebook as Variables, I will start to write my code. So, in the previous lecture, when we first actually installed the Jupyter Notebook, we have written some print arguments, right, like print HelloWorld. But if that was the first time that you have encountered with programming, maybe you got confused, okay, why are we printing, why are we just writing some things and getting some outputs? Actually, the programming is like that, like, if I type 5+6, and hit Run or Shift Enter, I got output of 11, right?

So, Python actually makes this calculation for me. If I just want Python to print something, it will print something, if I just Python to calculate something, it will calculate. I write code, and Python just take cares of the rest, and does the stuff for me. So if I want Python to go to the Internet and download me something, it will do so, and we will learn how to do that in future, of course.

But the general idea is that we want to sum like 1500 and 494, it will sum, okay. So, as you can see, we can do the basic Math, like if I divide 900/7, it will just divide and give me the exact number for it. Of course, we can do the other operations as well, like multiplication, or deduction, like 500 - 200, or, let's do some multiplication, so 120, so it's correct. So, there are other ways to just make this easy for us, like what?

So, for example, if I want to multiply 2 by three times, what do you do? Of course, you write, 2 * 2 * 2, and it will give me 8, but there is a shortcut to do that actually, and it goes like this, 2 ** 3, it means that 2 to the power 3, okay. Of course, in this case, it was simple. But, if I wanted to just multiply 2 by 9 times, it would have been much simpler for me to write it like this, so I get 512 as an output.

So, other than this, what can we do with Python in mathematical equations? There is a thing called remainder, for example. If I divide a number by another number, what is the remainder of that dividing operation? So, if you divide 10 by 2, what do you get as a remainder?

0, correct, because 2 * 5 is actually equal to 10. So, let's try this with Python, and remainder is shown as percentage. So, it goes like this, 10 % and 2, Shift Enter, it will give you an output of 0. So let's do another example, like 11% and 2, it will give you 1, of course, because the remainder of 11 divided by 2 is actually 1.

Let's do another example, 11 divided by 3, so the remainder will be 2, because 3 times 3 is equal to 9, and 11 - 9 is equal to 2, so the remainder is 2. I hope you know about this general simple mathematical concepts. This is very helpful when you want to find out a number is either an even number or an odd number. For example, if you get the remainder of it, and it returns 0, then it means that it's an even number.

Okay, enough for the mathematical operations, now let's talk about different kinds of numbers. So, as you can see, we had this decimal number in here, like 0.5, okay, if I just multiply 0.5 by 3, I get 1.5. So, we call this kind of numbers, Floats and Integers, okay. So Integer is a whole number, it doesn't have a decimal, so it's like 1, 2, 3, 6.

And, the other kind of that we were talking about was Float. So 1.5 is actually a Float, 0.4 is actually a Float. If I just multiply this two Floats, I get out of it Float, which is 1.20. And don't worry about this 00002 decimal thing, it is related to how Python actually interprets the decimal numbers, it won't cause any messy thing in your calculations, but it will just change the decimal, and in a little minor way, okay.

But, this is what you should know, whole numbers are Integers, decimal numbers are Floats, and they are two different kinds of datas in Python, okay. So, if someone, if some function, if some code expects you to give an Integer, you should give a whole number. If on the other hand, it expects a Float, you should give out a Float. So, maybe, it doesn't make sense to you right now, but it will as we move along.

All you have to do is just now understand that Integers are whole numbers, whereas the Floats are numbers with decimals. Now we're going to stop here, and within the next lecture, we're going to introduce a concept called variables, so that we won't have only pre-defined values, but also changing values as we move along with the code.
