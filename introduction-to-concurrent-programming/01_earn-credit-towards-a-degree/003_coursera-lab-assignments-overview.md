# Coursera Lab Assignments Overview

- **Course:** Introduction To Concurrent Programming
- **Module 1:** Earn credit towards a degree!
- **Lecture #:** 3
- **URL:** https://www.coursera.org/learn/introduction-to-concurrent-programming/lecture/3Chha/coursera-lab-assignments-overview
- **Extracted:** 2026-06-19 21:35:07

---

This video will go over how to utilize the course, our environment, to do laboratory activities and programming assignments. Below are six basic steps required to complete programming, lab activities and assignments, all of them in the browser. First, you will identify the lab or assignment that you wish to work on. Click the button with the name like Work in browser, which will launch the visual studio code editor in the course era lab environment.

Second, if required, open the directory associated with your module. For example the directory associated with this module would be named Module one third. To get lab assignment specific instructions, read any README.md text file in that directory. Fourth, edit any applicable code files, which will often have, .cpp, .hpp or .py extension.

The fifth step will be to open a terminal and use command line tools such as sh or make to clean, build and run your code one or more times until you are satisfied. Sixth and final step specific to assignments will be to edit.user and main code files to have the same value free user and then you can submit the assignment via buttons that will be at the bottom of your screen. These buttons will indicate which order they should be pressed to take all required steps. This is the page for your Module 1 programming assignment.

We will walk through all of the steps that you need to take to complete the assignment, focusing on the assignment content. There is an area for progress notification the button to start the lab activity, instructions on how to complete an assignment and submission results. Now let's click the Work in Browser button to start our assignment. Next, you will need to have the activity or assignments associated directory Open NVS code.

If you do not see the module number name near the top of the page, you will need to click the File menu option from the menu bar from inside the drop down menu, select Open. This will bring up the Open file or folder dialogue near the top of the editor. From the dialogue, you can navigate up the directory structure by clicking the double period. For this project, you will need to navigate to home codor, project, module1 in that order in the dialogue.

Once you have found the folder or file you need to open, you can click the OK, but the file or folder will open on the left side of the editor. You will now have all the files that you need to start on the lab or assignment for the current module. The next thing you should do is read the README.md text file, which will include background material and a set of assignment instructions. You can look at the run.sh File, which is a script that is run to execute all steps of the assignment.

The make file is used to clean, build and run your work. You should not need to modify these two files, but instead should modify Onley code and other associated files. If you're working on assignment next, you should ensure that the user file reflects your user, which could be any name or log in as long as it has no special characters. Another place to add it prior to even starting to write the code for the rest of assignment will be in your main code file.

In this case assignment.cpp. You will need to modify the user name string constant to reflect the same name that you've placed in the dot user file. By editing these two files, you will guarantee that your submission meets the first requirement of assignments. User name verification.

When you execute your code either directly or through run.sh or the make command, you will need to pass the same user name. The next step to submit your code is to click the Build Submission (Step 1) button at the bottom of the page. You will see a TERMINAL window at the bottom of the page, which will clean, build and execute your current code in output to a file, which is used in the grading of the assignment. Please note any output in the terminal.

Then you will click the Submit Assignment (Step 2) button, also near the bottom of the page, which submits that file to the greater. You can then look at the submission results in the assignment page. They will be amber until they're completed being run by the greater. When you open the submission results, you can see the grade whether you passed any feedback which is down here.

And if you're in a situation where things aren't working, you really want to look a little bit further, go into the Logs
