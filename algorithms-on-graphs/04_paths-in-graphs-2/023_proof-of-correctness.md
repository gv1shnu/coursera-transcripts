# Proof of Correctness

- **Course:** Algorithms On
- **Module 4:** Paths in Graphs 2
- **Lecture #:** 23
- **URL:** https://www.coursera.org/learn/algorithms-on-graphs/lecture/BcOfI/proof-of-correctness
- **Extracted:** 2026-06-21 21:26:38

---

Hi. In this video, we're going to prove that Dijkstra's algorithm indeed returns correct distances from the starting node to all the nodes in the graph. Let's look again at the pseudocode for the algorithm and here is the critical line. This is the line where we extract the minimum from the priority queue.

Or in other words, we take the node with the minimum dist value out of all the nodes outside of the known region and edit to the known region, saying that the dist value of this node is already equal to the correct distance to this node and we can now [inaudible] and relax all it's outgoing edges. The lemma states that when a node uses selected via ExtractMin from the priority queue, its dist value is already equal to the distance from the starting node to this node. This means that in the end, this dist value will also be equal to the distance because you remember that dist values are always the upper bounds on the correct distance because they are substantiated by a specific path which we can take to get these particular length of path to u which is equal to dist value of u. It can be smaller than the shortest possible path from A to u.

If at some point it's already equal to the shortest possible path of A to u, then it will not change anymore. Now we only need to prove this lemma. Let's assume for the sake of contradiction that this lemma is not true and at some moments for some graph, we will add a node to the known region. While it's dist value is different from the actual distance to this node.

The known region is on the left denoted by R and all the nodes in this region are marked with the black because they're already all known to us and all the nodes outside of the known region are to the right and they're colored with white. The node with the minimum dist value out of those nodes in this example has distance of six and let's assume for the sake of contradiction that this is not equal to the correct distance to D. We know that this is the upper bound. We know that there is some path from the starting node to D of length six.

If it's not the optimal path, then it means that there should be some path that is even shorter. This path should go from the known region to the unknown region at some point because the starting node is in the known region and the node D is in the unknown region so there should be some edge outgoing from the known region to some node in the unknown region. Let's assume, for example, that this is the edge from C to G. Then the shortest path from the starting node to C is already known and we assume that nothing is broken by this time yet so the dist value of the node C is equal to the length of the shortest path to C.

Now it means that the distance from the starting node to the node G in this path we're building to try to get to the node D and so the distance to G is already at least seven because this is the dist value of the node G so this is the best currently known path. As we've just came out of the known region, this path cannot be smaller than the best known path from the starting node to G. This first part of the path has length of at least seven. Then if we somehow get further from G to the node D, we will use one or more edges and all their weights are non-negative so the whole second part of the path, starting from node G and going somehow to node D will be non-negative.

The sum of the first part and the second part of the path will be at least seven and so it won't be shorter than the current dist value of the node D. It's already the minimum dist value of all the dist values in the unknown region. In the more general case, what I'm saying is that if node D has the minimum dist value out of all the nodes in the unknown region, then we cannot count to it shorter because at some point we need to get from known region to unknown region via some node which has dist value at least the same or bigger than the dist value of D. Then we will have to at least one or more edges which are non-negative and so there is no way to come to the node D shorter than by the current best path that we know.

This is contradiction with the assumption that the dist value of the node D is not equal to the actual distance and so we've proved that Dijkstra's algorithm will indeed return the correct values in the dist values of nodes. Also, we should somehow manage to find the shortest path itself, not just the distance, but this we can do the same way as we did for the breadth-first search algorithm and the shortest path tree, actually, the breadth values give us the same shortest path tree, but already in terms of the weighted edges. Because at any moment, breadth value of any node is either undefined or it points to the node through which the best currently known path goes to this node. As in the end the best currently known path in the dist value is equal to the actually shortest possible path.

That means that the breadth value for this node will contain indeed the previous node on the shortest path from the starting node to our node and so by going through breadth values from our node to its previous node, then to its previous node and so on, we will come to the starting node and that would be the shortest path reversed from the starting node to our nodes. The reconstruction procedure is completely the same as in the case of the breadth-first search and the breadth values is what contains the shortest path tree and what allows us to reconstruct the shortest paths from the starting node to all the other nodes in the graph. In the next video, we will finally analyze the running time of Dijkstra's algorithm depending on how to implement the priority queue in this algorithm.
