# Basics of Python Continue

- **Course:** Pyspark Python Hands On Guide Data Processing
- **Module 1:** Fundamentals of PySpark and Python
- **Lecture #:** 3
- **URL:** https://www.coursera.org/learn/pyspark-python-hands-on-guide-data-processing/lecture/nHY71/basics-of-python-continue
- **Extracted:** 2026-06-20 10:09:22

---

Now, we will see another one wherein we are saying my underscore list dot append and here we are giving an exclamation mark for appending. Now if we execute, it has been appended. Suppose you just say my underscore list and check whether it is being appended or not. Similarly, if you want to remove a particular item, you can say my underscore list dot you have the remove method and give there whatever character you want to or whatever item you want to remove.

Now again, if you say my underscore list, the item would be removed from the list. You can also remove an item using say del. Here you can say my underscore list and you're giving here the position zero colon one. You execute this and then check my underscore list, the item zero from one.

It would be removed. That is my, the first item. Suppose you want to do the list in the reverse order, then you can check my underscore list dot reverse method, which will do it in the reverse. So now if you execute my underscore list, you will get it in the reverse.

First will be nice is list. All items would be in reverse. Now again, you can append an item, for example, with another option. That is my underscore list dot you can say extend.

And here you give suppose exclamation mark and then check my underscore list again. Again item would be appended. Sub. Similarly, you have another method that is my underscore list dot pop minus one.

And now if you check my underscore list, that item would be removed. So minus one will be from the last item. Then suppose you want to insert at a particular position. So for that, you can say my underscore list dot you have insert method wherein you're giving at what position and what character you want to insert.

Now we are inserting at the position number zero. If I run this now, the first value will come the one which we have inserted now and the rest values would be pushed backward automatically. Similarly, if you want to sort, say my underscore list dot you use the sort method, it would be defaultly sorted in the ascending order. You can check it out.

Exclamation mark then is list and then nice. So now the items are automatically sorted. Similarly, you have some operations with strings which you can use. For example, we had the variable created my underscore str.

Here we are giving three in the bracket. We want to fetch what is the character at the position number three or we are saying my underscore str. We want to fetch from four till nine to get from four till nine, whatever values you have stored in it. Suppose you want to convert the given string into uppercase.

You have my underscore str dot upper. It converts to upper. Similarly, you have my underscore str dot lower, which converts to the lower case. If you want to know how many one particular character is, how many times it is repeated or what is the count of it?

So you can say my underscore str dot. You have the method name count and we are here we are giving a character W that is appearing once. For example, instead of this W, we are giving say T. So in that case, T is appearing here twice or I is appearing multiple times.

If you want to check for I, it's appearing again twice. Now after this, we'll check the replace. For example, you are saying my underscore str. You have the method that is replace and here you are giving replace E with say I.

So wherever E is occurring, it would be replaced with I over there. Or if you want to strip the white spaces. So you can say my underscore str dot strip. That is a method which will remove all the white spaces.

Now suppose you want to import certain libraries, you can say here import numpy. You can say import numpy as np for example. Then you can say give some selective import like from math. We want to import say pi.

That's another way in how you can import. Now for example, here we are creating some arrays. We are creating my underscore list is equal to say 1, 2, 3, 4 is what array I'm creating with some given default values. Now we are saying my underscore arr is equal to np dot array.

And here we are giving my underscore list, the one which we have already created. And then we create say my underscore 2d arr which is equal to np dot array. And here we are giving say 2d array. So the first one we are giving is 1, 2, 3 is the first value.

And second we are giving say 4, 5, 6 as the second value. We are creating a 2d array over here. Now selecting some numpy array elements, we can say my underscore arr, we want index number one, we're getting from index number one, whatever values you have stored from my underscore arr index number one is equal to the value two. Similarly, if you want to say selecting items at index number zero and one, you can say my underscore arr from index number zero till index number two, you get the value selected.

Now selecting a 2d numpy array, you can say my underscore 2d arr and here you want to fetch colon comma zero, you want to fetch the elements from zeroth element of the first array, it's coming one and four from both the one. Now, here, suppose we are checking my underscore arr, if it is more than three, you get the values false, then suppose we are giving my underscore arr, we multiply it by two, we get there the multiplication inside the values of the array, multiplied every value by two there. Then we are saying say my underscore arr plus we say np.array in bracket, we give 5, 6, 7, 8. For example, we executed now we have the array as 6, 8, 10 and 12.

That is what is the output we are getting here, the original value in the array, what you had created was 1, 2, 3, 4. 1, if you add it with 5, you get 6, 2, if you add it with 6, you get 8, 3, if you add with 7, you get 10 and 4, you add with 8, you get the value 12. Suppose you say my underscore arr dot shape. In this case, you will get what is the size of the array or the dimension of the array.

Suppose you say np.arr and in bracket, you say other underscore arr. Now here you have to give some different array to it. Suppose you give here np.array my underscore arr, you are adding the values into np.array. Similarly, you say np.insert and here you say my underscore arr comma 1 comma 5.

In this case, you are inserting 1 at index number 1, the value is 5. If you want to get the mean of the array, you can say np.mean my underscore arr, you get the mean of the array. Suppose you want the median my underscore arr or suppose you want to get the correlation coefficient of it, you can say my underscore arr dot corr coef, you get the correlation coefficient, it is my arr dot ef, it is like np.correlation coefficient in the bracket value. Similarly, np.next one will see np.standard deviation, here we give again my underscore arr.

Now to get the standard deviation of it. These were the simple basics of your Python that we have seen using or calling your PySpark. Now we will see some basics of PySpark that is your already basics and all in the next session. That's it from the session.

Thank you very much.
