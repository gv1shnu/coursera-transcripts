# More Examples on Mysql Part 1

- **Course:** Pyspark Python Hands On Guide Data Processing
- **Module 2:** Advanced Data Handling and Joins in PySpark
- **Lecture #:** 14
- **URL:** https://www.coursera.org/learn/pyspark-python-hands-on-guide-data-processing/lecture/MRUNx/more-examples-on-mysql-part-1
- **Extracted:** 2026-06-20 10:11:20

---

Now, in this session, we will see about MySQL with Spark SQL or PySpark. So we'll be learning how to connect the JDBC driver or data source with your PySpark data frames. So in case you are not familiar with Spark SQL, you can refer to some basics of MySQL that is how to create a database table and all and then we'll move on with the PySpark. So here, first of all, I am connected to MySQL command line client wherein I have given my root password and I have pressed enter.

Now first thing what we'll have to do is we'll have to create here or before that we'll have to use the database demo and then we'll have to create here a table name say stock price which will have date as varchar 20 then it will have symbol as varchar 10 then it will have open which is of type float then high again of type float then low again of type float close again of type float volume again of type float and adjacent close again of type float now here volume we are taking not float but bigint we'll create this table first now after we have created table instead of typing or inserting single single record in the table what we'll do is we'll import the data from a text file available and we'll export it or import it in our current data so that file is there in my C drive spark folder you can see this SP 500 HST is the text file which I have downloaded and this file has all the values delimited by comma this one will go first value will go for my date then symbol then the value will go for open then high then you will have low close and you will have volume all these values will first insert so for that what we'll have to do is we'll say load data local in file and we are giving here the path C spark filename will close this as it cannot be open it is SP 500 HST dot txt now this we are going to load into the table that we have created that is stock price fields terminated by comma and lines terminated by slash n that is a new line which means here we have told that fields end where you have a comma that means one field has ended second field start similarly a line will end wherever you will find a new line we press enter semicolon now after this it will import all the data from this text file into our database that is stock the stock price now if you have to check you can see select star from stock price you will get there all the records now this is these are all the records that we have selected what we'll do is we'll say limit in only the first 10 records would be viewable in this way we have added all the record now after this what we'll do is we'll go to our by spark session and now here we'll be connecting to the table that we have already given there so for that first thing that we'll have to do is import my SQL which will have all the classes you will require then we'll take data frame my SQL is equal to SQL context dot read dot format give your JDBC dot options and first thing will give us the URL now here the URL is JDBC colon my SQL slash slash colon 3306 is where it runs and your database is demo then you are giving driver is equal to and again in double quotes com dot my SQL dot JDBC dot driver then you give DB table is equal to and this time the table name is stock price then we give user is equal to root and last password is equal to root one two three then we say dot load method now once this all is loaded we'll have to just say data frame underscore my SQL dot show you get all the records that is only 20 records initially which are being displayed on your screen so in this way we have connected in with your ice park now for example here we say say symbols is equal to now we have taken your data frame underscore my SQL dot select we give your symbol as the column name and then we say symbols dot show so we get only for the column symbol similarly if you want to register a data frame as a table so first you will register the data frame as a table so that we can query the table normally so here we are seeing my your data frame is data frame underscore my SQL dot register m table and here we give say stock now this has been registered now query one will give select maximum volumes for stock of symbol a here we say result is equal to SQL context dot SQL and we give here say select max volume is the column as max from stock where symbol is equal to a and then we say result dot show so once you get all the values inside the variable result now let us see if we say result dot show see what is the max value is what we are getting inside the values similarly now suppose you want to view so we want to view the max volume and this stock group by symbol in that case we will say result is equal to SQL context dot SQL here you give select max volume as max from stock group by symbol close this and then you say result dot show so now in this case we'll get the values of it has to be something like this so result is equal to SQL context dot SQL here we give select max volume as max comma symbol from stock then say group by symbol and then you say here result dot show so now you will get symbol group that is GIS for this what is the max volume similarly say symbol K that's the max volume of it now we'll see another example where we'll use a text file and we'll count the number of words in that and we'll print the given output now first thing what we'll do is for word count before that we'll go to C drive see on desktop we have the folder python spark tutorial wherein I have one folder in wherein we have this word count 5 now whatever word is there in this suppose this word I want to count how many time it is appearing like here it is I can see one here it is I can see one but I don't have to scroll for that I wanted all the records to be displayed as it is I will require this path over here so first thing what we'll do is we'll say import from by spark we will import spark context and spark on after this we'll say conf is equal to spark on dot set the app name here we are giving say word count and we'll set the master as say local 3 after this we give lines is equal to sc dot text file and here now we will copy the path but don't forget to give here everywhere double slash that's my path where the file is from where I have to count each and individual word in then the file name is word underscore count dot you can give your txt also now after this we say words is equal to lines dot flat map and here we give the lambda line colon say line dot split space means the word has ended so in this case wherever we use space that means that is where we want to split it and give that in words then we give your word counts that is equal to words by count by value and then we say for say word comma count in word counts dot items we will run here a for loop and in this we'll give print print the values here colon here we are using here format as word and the count of it so here you see as soon as we press enter we get the count of each and individual word now we had seen for the word name great you can just scroll see it's appearing three times in the entire file so similar word with the similar case it will give you a print of it that's it from this simple example of a word count
